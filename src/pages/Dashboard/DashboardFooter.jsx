import { BottomNavigation } from "@mui/material";

import { StyledFooter } from "../../components/Navigation/StyledNavigationBar";

const DashboardFooter = ({ width1, width2 }) => {
  return <StyledFooter width1={width1} width2={width2} />;
};

export default DashboardFooter;
