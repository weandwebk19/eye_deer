import { Paper, Typography } from "@mui/material";

import { StyledPaper } from "components/Paper";

const PresentationSlide = () => {
  return (
    <StyledPaper sx={{ pb: "56.25%" }}>
      <Typography>Chart here</Typography>
    </StyledPaper>
  );
};

export default PresentationSlide;
